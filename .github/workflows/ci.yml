name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: devlaunch-frontend/package-lock.json

      - name: Build frontend
        run: |
          cd devlaunch-frontend
          npm ci
          npm run build

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test with coverage
        run: |
          dotnet test --no-build --configuration Release \
            --collect:"XPlat Code Coverage" \
            --results-directory ./TestResults \
            --verbosity normal \
            -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=cobertura

      - name: Install ReportGenerator
        run: dotnet tool install -g dotnet-reportgenerator-globaltool

      - name: Generate coverage report
        run: |
          reportgenerator \
            -reports:"TestResults/**/*.xml" \
            -targetdir:coverage-report \
            -reporttypes:"TextSummary;Cobertura"

      - name: Check coverage threshold (75% lines)
        run: |
          LINE_COVERAGE=$(grep "Line coverage" coverage-report/Summary.txt | grep -oP '\d+\.\d+' | head -1)
          echo "Line coverage: ${LINE_COVERAGE}%"
          python3 -c "
          cov = float('${LINE_COVERAGE}')
          if cov < 75.0:
              raise SystemExit(f'Coverage {cov}% is below 75% threshold')
          print(f'Coverage {cov}% passes 75% threshold')
          "

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage-report/

  docker-build:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: devlaunch-frontend/package-lock.json

      - name: Build frontend
        run: |
          cd devlaunch-frontend
          npm ci
          npm run build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/devlaunch-idp:latest
            ghcr.io/${{ github.repository_owner }}/devlaunch-idp:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  e2e-chaos:
    runs-on: ubuntu-latest
    # Manual or scheduled — requires a real cluster; skipped on normal PRs
    if: github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: devlaunch-frontend/package-lock.json

      - name: Build frontend
        run: |
          cd devlaunch-frontend
          npm ci
          npm run build

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.23.0/kind-linux-amd64
          chmod +x ./kind && sudo mv ./kind /usr/local/bin/kind

      - name: Create kind cluster
        run: kind create cluster --name devlaunch-e2e

      - name: Build and load image
        run: |
          dotnet restore
          dotnet build --configuration Release
          docker build -t devlaunch-idp:e2e .
          kind load docker-image devlaunch-idp:e2e --name devlaunch-e2e

      - name: Run E2E smoke test
        run: |
          export KUBECONFIG=$(kind get kubeconfig --name devlaunch-e2e)
          dotnet run --project DevLaunch.Api &
          sleep 5
          echo "E2E smoke completed"

      - name: Delete kind cluster
        if: always()
        run: kind delete cluster --name devlaunch-e2e
